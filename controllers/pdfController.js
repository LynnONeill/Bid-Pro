const createHTML = require("../createHTML.js")

let customerObject = {
    name: name,
    address: address,
    city: city,
    state: state,
    zip: zip,
    phone: phone,
    email, email
}

let productObject = {
    name: name
}





module.exports = {
    createHTML: createPDF
};